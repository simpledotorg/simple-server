<div class="page-header">
  <h1>Error Traces</h2>
</div>

<div class="card">
  <div class="row">
    <div class="col-6">

      <div class="form-group">
        <label>
          Standard web request error
        </label>

        <div class="form-text text-muted mb-2">
          This will raise an error directly from the action, so you will see a 500 page after clicking the button.
          That is expected.
        </div>

        <%= button_to "Create error", admin_error_traces_path, class: "btn btn-outline-danger" %>
      </div>
    </div>
  </div>
</div>

<div class="card">
  <div class="row">
    <div class="col-6">

      <div class="form-group">
        <label>
          Background job error
        </label>

        <div class="form-text text-muted mb-2">
          This will raise an error from a sidekiq job.
        </div>

        <%= button_to "Create error", admin_error_traces_path(type: :job), class: "btn btn-outline-danger" %>
      </div>
    </div>
  </div>
</div>