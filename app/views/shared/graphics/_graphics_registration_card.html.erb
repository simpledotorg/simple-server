<div class="card" style="margin-top: -2px;">
  <table class="snapshot-table">
    <thead>
    <tr>
      <th></th>
      <th><%= t('dashboard.graphics.new_patients_registered') %></th>
      <th class="pl-3"><%= t('dashboard.graphics.patients_called_for_followup') %></th>
    </tr>
    </thead>
    <tbody>
    <% cohort_analytics.sort_by { |k, v| k }.reverse.each do |date, quarter_analytics| %>
      <tr>
        <td class="snapshot-quarter"><%= quarter_string(date) %></td>
        <td class="green text-right pr-3" style="border-right: 1px solid #dce4e8;"><%= quarter_analytics[:registered] %></td>
        <td class="green text-right pl-3 pr-3"><%= quarter_analytics[:followed_up] %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>