<%= render 'shared/graphics/graphics_partial',
           cohort_analytics: @cohort_analytics,
           dashboard_analytics: @dashboard_analytics,
           organization_name: @region.organization.name,
           name: @region.name,
           quarter: @quarter,
           year: @year %>
