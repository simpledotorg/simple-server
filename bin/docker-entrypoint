#!/usr/bin/env bash

set -e

# Set environment variables system-wide.
# Mainly for cron jobs.
env >> /etc/environment

# Test entrypoint script
echo "Running entrypoint script..." > /var/tmp/entrypoint.log

exec "$@"
